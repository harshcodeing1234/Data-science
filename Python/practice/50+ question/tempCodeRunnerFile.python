import pandas as pd

info = {
    'Name':['Harsh','Prashant','Ravi'],
    'Age': ['20','19','21']
}

df = pd.DataFrame(info , index = False)

# Save to Excel
# df.to_excel("output.xlsx", index=False)
print(df)
